.listItem {
    position: relative;
    list-style-type: none;
}

.showMarker {
    padding-left: 4.6rem;
}

.showMarker::before {
    content: '';
    position: absolute;
    top: 3rem;
    left: 0;
    height: 1.8rem;
    width: 1.8rem;
    border: 0.2rem solid var(--accent-color);
    border-radius: 50%;
    box-sizing: border-box;
}
